# 验证记录 · 2026-09-07

- Windows 本机 .NET 8 Release 编译成功，0 警告、0 错误。
- win-x64 自包含单文件发布成功。
- TimerEngine 的 10 项测试全部通过：实际截止时间计时、暂停、恢复后结算、完成事件去重、长时间跳跃不重复结算、短休息转换、第四轮长休息、休息不计数、重置、阶段时长切换。
- 使用 Windows computer-use 实际启动发布版，检查主界面、AI 插画和图标、统计空状态、设置表单。
- 实际点击开始，观察 25:00 递减到 24:49；点击暂停，按钮变为继续，时间停留 24:36。
- 根据截图修复圆环在受限高度下裁切，改为 Viewbox 缩放；修复按钮前景色继承、插画圆角裁切。

尚未进行其他电脑、ARM64、各种显示缩放比例的兼容测试；未用 UI 等待完整 25 分钟周期、手工完成 CSV 文件保存和模拟系统睡眠。相关计时语义以独立逻辑测试覆盖。未进行安装器或数字签名流程。


## 1.1 强制休息验证

- 16 项逻辑检查全部通过，新增完整休息奖励、重复结算、预览、紧急退出、旧数据默认迁移、专注截止瞬间暂停竞争条件。
- 两种场景均在本机执行 8 秒真实全屏/键盘钩子集成测试，自动结束并释放钩子。测试不读取或写入用户统计。
- 测试期间调用普通 Close，窗口保持显示；结束后通过 Release 正常关闭。
- 两种场景均生成自身 WPF 渲染图并视觉检查。果子随进度缩放，小人随时间循环摆动、弹跳与轻微拉伸。
- 多显示器按 Screen.AllScreens 实现且支持显示器变更重建，尚未在多显示器混合 DPI 硬件上实测。
- 未逐一人工验证所有物理键盘快捷键组合、系统安全桌面来回切换及完整长休息周期。保留系统安全操作，不承诺不可绕过的系统锁屏。


## 1.2 引导场景验证

- 22 项逻辑检查全部通过。新增场景保存 ID、未知 ID 回退、远眺阶段至少 20 秒、眨眼阶段切换、轻活动后安静休息、不要求屏息。
- Eyes、Move、Calm 三种场景各执行 8 秒全屏集成测试，拒绝普通关闭，自动关闭并释放键盘钩子；不写入用户数据。
- 已检查三种场景自身 WPF 渲染图，文字无截断、AI 陪伴素材正常显示。
- 引导阶段切换由纯逻辑测试覆盖，未在 UI 中等待完整 50 秒活动序列；未新增语音播报或运动动作识别。
